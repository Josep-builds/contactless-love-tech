NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key: used ONLY by the escalation Edge Function / server-side
# cron job. Never expose to the browser, never commit a real value.
SUPABASE_SERVICE_ROLE_KEY=

# Hours of inactivity before an open/in_progress case auto-escalates.
ESCALATION_WINDOW_HOURS=48
